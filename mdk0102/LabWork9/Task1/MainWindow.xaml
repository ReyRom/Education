<Window x:Class="Task1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task1"
        mc:Ignorable="d"
        Title="MainWindow" Height="150" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="Логин" />
        <Label Content="EMail" Grid.Row="1" />
        <TextBox Grid.Column="1" />
        <TextBox Grid.Column="1" Grid.Row="1" />
        <CheckBox x:Name="AcceptCheckBox" Content="Я согласен на получение рассылки" Grid.Row="2" Grid.ColumnSpan="2" Checked="AcceptCheckBox_Checked" Unchecked="AcceptCheckBox_Checked"/>
        <Button x:Name="AcceptButton" Content="Подписаться" Grid.Row="3" Grid.ColumnSpan="2" IsEnabled="False"/>

    </Grid>
</Window>
