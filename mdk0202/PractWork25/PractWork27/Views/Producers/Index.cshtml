@model IEnumerable<PractWork25.Models.Producer>

@{
    ViewBag.Title = "Index";
}

<h2>Производители</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Country)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Country)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdProducer }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdProducer }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdProducer })
        </td>
    </tr>
}

</table>
